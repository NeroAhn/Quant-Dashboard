---
phase: 02-dashboard-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ActionBadge.tsx
  - src/components/ui/RevisionBadge.tsx
  - src/components/ui/SearchBar.tsx
  - src/components/ui/SortHeader.tsx
  - src/components/tables/StockTable.tsx
  - src/components/tables/WatchlistTab.tsx
  - src/components/tables/MonitoringTab.tsx
autonomous: true
requirements:
  - TAB2-01
  - TAB2-02
  - TAB2-03
  - TAB2-04
  - TAB2-05
  - TAB2-06
  - TAB2-07
  - DSGN-04

must_haves:
  truths:
    - "Watchlist tab renders 12 tickers in a table with all 9 columns"
    - "Monitoring tab renders 9 tickers in a table with all 9 columns"
    - "User can click column headers to sort by RS, drawdown, MA50 Dist, or symbol"
    - "Sort state persists across tab switches via Zustand"
    - "User can type in search bar to filter symbols"
    - "Search state persists across tab switches via Zustand"
    - "RS > 110 shows green text, RS < 90 shows red text"
    - "Revision UP/DOWN/NEUTRAL displays as colored badges"
    - "Tables transform to card view on mobile screens"
    - "Strategic Note column exists showing '-' placeholder"
  artifacts:
    - path: "src/components/ui/ActionBadge.tsx"
      provides: "Buy/Trim/Wait/Hold colored badge"
      exports: ["ActionBadge"]
    - path: "src/components/ui/RevisionBadge.tsx"
      provides: "UP/DOWN/NEUTRAL colored badge"
      exports: ["RevisionBadge"]
    - path: "src/components/ui/SearchBar.tsx"
      provides: "Symbol search input wired to Zustand"
      exports: ["SearchBar"]
    - path: "src/components/ui/SortHeader.tsx"
      provides: "Sortable column header with arrow icons"
      exports: ["SortHeader"]
    - path: "src/components/tables/StockTable.tsx"
      provides: "Shared table for both Watchlist and Monitoring"
      exports: ["StockTable"]
    - path: "src/components/tables/WatchlistTab.tsx"
      provides: "Watchlist tab wrapper passing 12 tickers"
      exports: ["WatchlistTab"]
    - path: "src/components/tables/MonitoringTab.tsx"
      provides: "Monitoring tab wrapper passing 9 tickers"
      exports: ["MonitoringTab"]
  key_links:
    - from: "src/components/tables/StockTable.tsx"
      to: "src/store/dashboard.ts"
      via: "useDashboardStore for searchQuery and sortConfig"
      pattern: "useDashboardStore"
    - from: "src/components/tables/WatchlistTab.tsx"
      to: "src/hooks/use-watchlist.ts"
      via: "useWatchlist hook for data fetching"
      pattern: "useWatchlist"
    - from: "src/components/tables/MonitoringTab.tsx"
      to: "src/hooks/use-monitoring.ts"
      via: "useMonitoring hook for data fetching"
      pattern: "useMonitoring"
    - from: "src/components/ui/SearchBar.tsx"
      to: "src/store/dashboard.ts"
      via: "useDashboardStore.setSearchQuery"
      pattern: "setSearchQuery"
---

<objective>
Build the shared StockTable component system with sorting, searching, mobile card view, and the Watchlist/Monitoring tab wrappers.

Purpose: Implements D-02 (single shared table component) with full interactivity -- sortable columns (D-07), symbol search (D-08), Strategic Note column (D-09), RS/Revision color coding (TAB2-06, TAB2-07), and mobile card view (D-06). The table is the core data visualization for the dashboard.

Output: 7 files -- 4 reusable UI primitives (ActionBadge, RevisionBadge, SearchBar, SortHeader) and 3 table components (shared StockTable, WatchlistTab wrapper, MonitoringTab wrapper).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-ui/02-CONTEXT.md
@.planning/phases/02-dashboard-ui/02-RESEARCH.md

@src/app/page.tsx
@src/store/dashboard.ts
@src/types/dashboard.ts
@src/hooks/use-watchlist.ts
@src/hooks/use-monitoring.ts
@src/components/skeleton.tsx

<interfaces>
<!-- Types from dashboard.ts -->
From src/types/dashboard.ts:
```typescript
export type ActionSignal = "Buy" | "Trim" | "Wait" | "Hold";
export type Revision = "UP" | "DOWN" | "NEUTRAL";

export interface WatchlistItem extends QuantMetrics {
  symbol: string;
  price: number | null;
  change1D: number | null;
  high52w: number | null;
  forwardPE: number | null;
  error?: string;
}

export interface WatchlistResponse {
  data: (WatchlistItem | { symbol: string; error: string })[];
  timestamp: string;
}
```

<!-- Zustand store -->
From src/store/dashboard.ts:
```typescript
type SortField = "rs" | "drawdown" | "ma50Dist" | "symbol";
type SortDirection = "asc" | "desc";

interface DashboardState {
  searchQuery: string;
  setSearchQuery: (query: string) => void;
  sortConfig: { field: SortField; direction: SortDirection };
  setSortConfig: (field: SortField) => void;
}
```

<!-- TanStack Query hooks -->
From src/hooks/use-watchlist.ts:
```typescript
export function useWatchlist(): UseQueryResult<WatchlistResponse>;
```

From src/hooks/use-monitoring.ts:
```typescript
export function useMonitoring(): UseQueryResult<WatchlistResponse>;
```

<!-- Colors (will be created by Plan 01, but Plan 02 can run in parallel) -->
Colors to create locally if Plan 01 hasn't run yet, or import from @/lib/colors:
```typescript
const ACTION_COLORS: Record<ActionSignal, string> = {
  Buy: "bg-green-100 text-green-700",
  Trim: "bg-orange-100 text-orange-700",
  Wait: "bg-red-100 text-red-700",
  Hold: "bg-slate-100 text-slate-600",
};
const REVISION_COLORS: Record<Revision, string> = {
  UP: "bg-green-100 text-green-700",
  DOWN: "bg-red-100 text-red-700",
  NEUTRAL: "bg-slate-100 text-slate-500",
};
```

<!-- Skeleton components -->
From src/components/skeleton.tsx:
```typescript
export function TableSkeleton({ rows }: { rows?: number }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI primitive components -- ActionBadge, RevisionBadge, SearchBar, SortHeader</name>
  <files>src/components/ui/ActionBadge.tsx, src/components/ui/RevisionBadge.tsx, src/components/ui/SearchBar.tsx, src/components/ui/SortHeader.tsx</files>
  <read_first>
    - src/app/page.tsx (lines 27-38 for color definitions, lines 511-520 for RS color logic, lines 536-540 for revision badge rendering, lines 550-556 for action badge rendering)
    - src/store/dashboard.ts (SortField type, setSortConfig, searchQuery, setSearchQuery)
    - src/types/dashboard.ts (ActionSignal, Revision types)
    - src/lib/colors.ts (if exists from Plan 01 -- if not, define colors inline)
  </read_first>
  <action>
Create 4 UI primitive components. Each must have `"use client"` directive.

**Important:** Check if `src/lib/colors.ts` exists (from Plan 01). If it does, import `ACTION_COLORS`, `REVISION_COLORS`, `getRsColor`, etc. from `@/lib/colors`. If it doesn't exist yet (parallel execution), define the color constants inline in each component file. The Plan 03 page.tsx rewrite will normalize all imports.

**1. `src/components/ui/ActionBadge.tsx`** (per ASIG-05, TAB2-06):

```typescript
"use client";
import type { ActionSignal } from "@/types/dashboard";

const ACTION_COLORS: Record<ActionSignal, string> = {
  Buy: "bg-green-100 text-green-700",
  Trim: "bg-orange-100 text-orange-700",
  Wait: "bg-red-100 text-red-700",
  Hold: "bg-slate-100 text-slate-600",
};

export function ActionBadge({ action }: { action: ActionSignal }) {
  return (
    <span className={`text-[10px] px-2 py-1 rounded font-bold ${ACTION_COLORS[action]}`}>
      {action}
    </span>
  );
}
```

If `src/lib/colors.ts` exists, replace the inline `ACTION_COLORS` with: `import { ACTION_COLORS } from "@/lib/colors";`

**2. `src/components/ui/RevisionBadge.tsx`** (per TAB2-07):

```typescript
"use client";
import type { Revision } from "@/types/dashboard";

const REVISION_COLORS: Record<Revision, string> = {
  UP: "bg-green-100 text-green-700",
  DOWN: "bg-red-100 text-red-700",
  NEUTRAL: "bg-slate-100 text-slate-500",
};

export function RevisionBadge({ revision }: { revision: Revision }) {
  return (
    <span className={`px-1.5 py-0.5 rounded-full font-bold uppercase text-[9px] ${REVISION_COLORS[revision]}`}>
      {revision}
    </span>
  );
}
```

If `src/lib/colors.ts` exists, import `REVISION_COLORS` from it instead.

**3. `src/components/ui/SearchBar.tsx`** (per D-08, TAB2-05, DSGN-05, DSGN-06):

```typescript
"use client";
import { Search } from "lucide-react";
import { useDashboardStore } from "@/store/dashboard";

export function SearchBar() {
  const { searchQuery, setSearchQuery } = useDashboardStore();

  return (
    <div className="relative">
      <Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" />
      <input
        type="text"
        placeholder="종목 검색..."
        value={searchQuery}
        onChange={(e) => setSearchQuery(e.target.value)}
        className="pl-9 pr-4 py-2 text-sm border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent w-full md:w-64"
      />
    </div>
  );
}
```

**4. `src/components/ui/SortHeader.tsx`** (per D-07, TAB2-04, DSGN-06):

```typescript
"use client";
import { ArrowUpDown, ArrowUp, ArrowDown } from "lucide-react";
import { useDashboardStore } from "@/store/dashboard";

type SortField = "rs" | "drawdown" | "ma50Dist" | "symbol";

interface SortHeaderProps {
  field: SortField;
  label: string;
  className?: string;
}

export function SortHeader({ field, label, className = "" }: SortHeaderProps) {
  const { sortConfig, setSortConfig } = useDashboardStore();
  const isActive = sortConfig.field === field;

  return (
    <th
      className={`px-6 py-4 cursor-pointer select-none hover:bg-slate-200 transition-colors ${className}`}
      onClick={() => setSortConfig(field)}
    >
      <span className="flex items-center gap-1">
        {label}
        {isActive ? (
          sortConfig.direction === "asc" ? <ArrowUp size={12} /> : <ArrowDown size={12} />
        ) : (
          <ArrowUpDown size={12} className="text-slate-300" />
        )}
      </span>
    </th>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && test -f src/components/ui/ActionBadge.tsx && test -f src/components/ui/RevisionBadge.tsx && test -f src/components/ui/SearchBar.tsx && test -f src/components/ui/SortHeader.tsx && grep -c "useDashboardStore" src/components/ui/SearchBar.tsx && grep -c "setSortConfig" src/components/ui/SortHeader.tsx && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/ActionBadge.tsx exports `ActionBadge` with `action: ActionSignal` prop
    - src/components/ui/RevisionBadge.tsx exports `RevisionBadge` with `revision: Revision` prop
    - src/components/ui/SearchBar.tsx imports `useDashboardStore` and calls `setSearchQuery`
    - src/components/ui/SearchBar.tsx contains placeholder "종목 검색..."
    - src/components/ui/SortHeader.tsx imports `useDashboardStore` and calls `setSortConfig`
    - src/components/ui/SortHeader.tsx imports ArrowUpDown, ArrowUp, ArrowDown from lucide-react
    - All 4 files have `"use client"` directive
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>4 UI primitive components created. ActionBadge renders color-coded action signals. RevisionBadge renders UP/DOWN/NEUTRAL badges. SearchBar wires to Zustand searchQuery with Korean placeholder. SortHeader provides clickable column headers with directional arrow icons wired to Zustand sortConfig. All compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared StockTable + WatchlistTab and MonitoringTab wrappers</name>
  <files>src/components/tables/StockTable.tsx, src/components/tables/WatchlistTab.tsx, src/components/tables/MonitoringTab.tsx</files>
  <read_first>
    - src/app/page.tsx (lines 429-706 -- the two table sections to extract and unify)
    - src/components/ui/ActionBadge.tsx (just created in Task 1)
    - src/components/ui/RevisionBadge.tsx (just created in Task 1)
    - src/components/ui/SearchBar.tsx (just created in Task 1)
    - src/components/ui/SortHeader.tsx (just created in Task 1)
    - src/store/dashboard.ts (searchQuery, sortConfig for filtering/sorting)
    - src/types/dashboard.ts (WatchlistItem type -- check all fields)
    - src/hooks/use-watchlist.ts (useWatchlist hook)
    - src/hooks/use-monitoring.ts (useMonitoring hook -- verify it exists and returns same shape)
    - src/components/skeleton.tsx (TableSkeleton import)
    - .planning/phases/02-dashboard-ui/02-CONTEXT.md (D-02 shared table, D-06 mobile card view, D-09 Strategic Note)
  </read_first>
  <action>
**1. `src/components/tables/StockTable.tsx`** (per D-02, D-06, D-07, D-08, D-09, TAB2-03, TAB2-04, TAB2-05, TAB2-06, TAB2-07, DSGN-04):

This is the core shared table component used by both Watchlist and Monitoring tabs. It handles:
- Filtering by `searchQuery` from Zustand
- Sorting by `sortConfig` from Zustand (numeric subtraction for rs/drawdown/ma50Dist, localeCompare for symbol)
- Desktop table view (hidden on mobile with `hidden md:block`)
- Mobile card view (hidden on desktop with `md:hidden`)
- All 9 columns: Symbol, Price/1D%, High/Drawdown, RS, MA50 Dist, Revision, FWD P/E, Strategic Note (D-09), Action
- `isFullItem` type guard to filter out partial error items

```typescript
"use client";

import { useDashboardStore } from "@/store/dashboard";
import { TableSkeleton } from "@/components/skeleton";
import { ActionBadge } from "@/components/ui/ActionBadge";
import { RevisionBadge } from "@/components/ui/RevisionBadge";
import { SearchBar } from "@/components/ui/SearchBar";
import { SortHeader } from "@/components/ui/SortHeader";
import type { WatchlistItem, WatchlistResponse } from "@/types/dashboard";
import type { ReactNode } from "react";

// Import color utilities if available, otherwise inline
// Check for @/lib/colors existence -- if Plan 01 ran, import from there
// Otherwise use inline functions:
function getRsColor(rs: number): string {
  if (rs > 110) return "text-green-600";
  if (rs < 90) return "text-red-600";
  return "text-slate-600";
}

function getMa50Color(dist: number): string {
  if (dist > 10) return "text-orange-600";
  if (dist < -5) return "text-green-600";
  return "text-slate-600";
}

function getDrawdownColor(dd: number): string {
  if (dd < -10) return "text-red-500";
  return "text-slate-600";
}

function getChangeColor(change: number | null): string {
  if (change == null) return "text-slate-400";
  return change >= 0 ? "text-green-600" : "text-red-600";
}

function isFullItem(
  item: { symbol: string; error?: string } | WatchlistItem
): item is WatchlistItem {
  return "price" in item;
}

interface StockTableProps {
  title: string;
  description: string;
  icon: ReactNode;
  isPending: boolean;
  error: Error | null;
  data: WatchlistResponse | undefined;
  defaultRows: number; // 12 for watchlist, 9 for monitoring
}

export function StockTable({ title, description, icon, isPending, error, data, defaultRows }: StockTableProps) {
  const { searchQuery, sortConfig } = useDashboardStore();

  const items = (data?.data ?? []).filter(isFullItem);

  // Filter by search query (TAB2-05)
  const filtered = items.filter((item) =>
    item.symbol.toLowerCase().includes(searchQuery.toLowerCase())
  );

  // Sort (TAB2-04) -- handle string vs number fields
  const sorted = [...filtered].sort((a, b) => {
    const mul = sortConfig.direction === "asc" ? 1 : -1;
    const field = sortConfig.field;

    if (field === "symbol") {
      return a.symbol.localeCompare(b.symbol) * mul;
    }

    const aVal = a[field] as number;
    const bVal = b[field] as number;
    return (aVal - bVal) * mul;
  });

  return (
    <div className="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden">
      {/* Header with title and search */}
      <div className="p-6 border-b border-slate-100 flex flex-col md:flex-row md:justify-between md:items-center gap-4 bg-slate-50/50">
        <div>
          <h2 className="text-xl font-bold flex items-center gap-2">
            {icon}
            {title}
          </h2>
          <p className="text-xs text-slate-500 mt-1">{description}</p>
        </div>
        <SearchBar />
      </div>

      {/* Loading state */}
      {isPending && (
        <div className="p-6">
          <TableSkeleton rows={defaultRows} />
        </div>
      )}

      {/* Error state */}
      {error && (
        <p className="p-6 text-red-500">Error: {error.message}</p>
      )}

      {/* Desktop table view (hidden on mobile per D-06) */}
      {sorted.length > 0 && (
        <div className="hidden md:block overflow-x-auto">
          <table className="w-full text-left">
            <thead className="bg-slate-100 text-slate-500 text-[10px] uppercase font-bold">
              <tr>
                <SortHeader field="symbol" label="종목" />
                <th className="px-6 py-4">가격 / 1D %</th>
                <th className="px-6 py-4">고가 / 낙폭</th>
                <SortHeader field="rs" label="RS (상대강도)" />
                <SortHeader field="ma50Dist" label="MA50 이격도" />
                <th className="px-6 py-4">Revision</th>
                <th className="px-6 py-4">FWD P/E</th>
                <th className="px-6 py-4">전략 메모</th>
                <th className="px-6 py-4">액션</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-slate-100">
              {sorted.map((t) => (
                <tr key={t.symbol} className="hover:bg-slate-50 transition-colors text-xs">
                  <td className="px-6 py-4 font-bold text-sm">{t.symbol}</td>
                  <td className="px-6 py-4">
                    <div className="flex flex-col">
                      <span className="font-mono font-bold">${t.price}</span>
                      <span className={`font-semibold ${getChangeColor(t.change1D)}`}>
                        {t.change1D != null
                          ? `${t.change1D >= 0 ? "+" : ""}${t.change1D.toFixed(2)}%`
                          : "N/A"}
                      </span>
                    </div>
                  </td>
                  <td className="px-6 py-4">
                    <div className="flex flex-col gap-1">
                      <span className="text-slate-400 font-mono">${t.high52w ?? "N/A"}</span>
                      <span className={`font-bold ${getDrawdownColor(t.drawdown)}`}>
                        {t.drawdown.toFixed(2)}%
                      </span>
                    </div>
                  </td>
                  <td className="px-6 py-4">
                    <span className={`font-bold ${getRsColor(t.rs)}`}>
                      {t.rs.toFixed(1)}
                    </span>
                  </td>
                  <td className={`px-6 py-4 font-mono font-bold ${getMa50Color(t.ma50Dist)}`}>
                    {t.ma50Dist >= 0 ? "+" : ""}{t.ma50Dist.toFixed(2)}%
                  </td>
                  <td className="px-6 py-4">
                    <RevisionBadge revision={t.revision} />
                  </td>
                  <td className="px-6 py-4 text-slate-600 font-medium">
                    {t.forwardPE != null ? `${t.forwardPE.toFixed(1)}x` : "N/A"}
                  </td>
                  <td className="px-6 py-4 text-slate-400 text-[10px]">-</td>
                  <td className="px-6 py-4">
                    <ActionBadge action={t.action} />
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}

      {/* Mobile card view (hidden on desktop per D-06) */}
      {sorted.length > 0 && (
        <div className="md:hidden space-y-3 p-4">
          {sorted.map((t) => (
            <div key={t.symbol} className="bg-white rounded-xl border border-slate-200 p-4 space-y-3 shadow-sm">
              {/* Header row: Symbol + Action badge */}
              <div className="flex justify-between items-center">
                <span className="font-bold text-sm">{t.symbol}</span>
                <ActionBadge action={t.action} />
              </div>
              {/* Primary metrics: 2-column grid */}
              <div className="grid grid-cols-2 gap-2 text-xs">
                <div>
                  <span className="text-slate-400 block">가격</span>
                  <span className="font-mono font-bold">${t.price}</span>
                  <span className={`ml-1 font-semibold ${getChangeColor(t.change1D)}`}>
                    {t.change1D != null ? `${t.change1D >= 0 ? "+" : ""}${t.change1D.toFixed(2)}%` : ""}
                  </span>
                </div>
                <div>
                  <span className="text-slate-400 block">RS</span>
                  <span className={`font-bold ${getRsColor(t.rs)}`}>{t.rs.toFixed(1)}</span>
                </div>
                <div>
                  <span className="text-slate-400 block">낙폭</span>
                  <span className={`font-bold ${getDrawdownColor(t.drawdown)}`}>{t.drawdown.toFixed(2)}%</span>
                </div>
                <div>
                  <span className="text-slate-400 block">MA50 이격도</span>
                  <span className={`font-mono font-bold ${getMa50Color(t.ma50Dist)}`}>
                    {t.ma50Dist >= 0 ? "+" : ""}{t.ma50Dist.toFixed(2)}%
                  </span>
                </div>
              </div>
              {/* Secondary metrics row */}
              <div className="flex items-center gap-3 text-[10px] text-slate-500 pt-1 border-t border-slate-100">
                <RevisionBadge revision={t.revision} />
                <span>P/E {t.forwardPE != null ? `${t.forwardPE.toFixed(1)}x` : "N/A"}</span>
                <span>고가 ${t.high52w ?? "N/A"}</span>
              </div>
            </div>
          ))}
        </div>
      )}

      {/* Empty state after filtering */}
      {!isPending && !error && sorted.length === 0 && items.length > 0 && (
        <div className="p-6 text-center text-slate-400 text-sm">
          &quot;{searchQuery}&quot;에 해당하는 종목이 없습니다.
        </div>
      )}
    </div>
  );
}
```

IMPORTANT: If `src/lib/colors.ts` exists (from Plan 01 running first), replace the inline `getRsColor`, `getMa50Color`, `getDrawdownColor`, `getChangeColor` functions with imports: `import { getRsColor, getMa50Color, getDrawdownColor, getChangeColor } from "@/lib/colors";`

**2. `src/components/tables/WatchlistTab.tsx`** (per TAB2-01, D-02):

```typescript
"use client";
import { Activity } from "lucide-react";
import { useWatchlist } from "@/hooks/use-watchlist";
import { StockTable } from "./StockTable";

export function WatchlistTab() {
  const watchlist = useWatchlist();

  return (
    <StockTable
      title="워치리스트 (Primary Matrix)"
      description="12 Priority Tickers | Fundamental + Technical + Sentiment"
      icon={<Activity className="text-brand-accent-blue" size={24} />}
      isPending={watchlist.isPending}
      error={watchlist.error}
      data={watchlist.data}
      defaultRows={12}
    />
  );
}
```

Note: The icon uses `text-brand-accent-blue` which requires Plan 01's globals.css update. If Plan 01 hasn't run, fall back to `text-blue-600`. Check `src/app/globals.css` for `--color-brand-accent-blue` before deciding. The Plan 03 rewrite will normalize.

**3. `src/components/tables/MonitoringTab.tsx`** (per TAB2-02, D-02):

```typescript
"use client";
import { Layers } from "lucide-react";
import { useMonitoring } from "@/hooks/use-monitoring";
import { StockTable } from "./StockTable";

export function MonitoringTab() {
  const monitoring = useMonitoring();

  return (
    <StockTable
      title="모니터링 리스트 (Expansion Set)"
      description="9 Additional Tickers | Secondary Monitoring Matrix"
      icon={<Layers className="text-purple-600" size={24} />}
      isPending={monitoring.isPending}
      error={monitoring.error}
      data={monitoring.data}
      defaultRows={9}
    />
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && test -f src/components/tables/StockTable.tsx && test -f src/components/tables/WatchlistTab.tsx && test -f src/components/tables/MonitoringTab.tsx && grep -c "searchQuery" src/components/tables/StockTable.tsx && grep -c "sortConfig" src/components/tables/StockTable.tsx && grep -c "md:hidden" src/components/tables/StockTable.tsx && grep -c "전략 메모" src/components/tables/StockTable.tsx && grep -c "useWatchlist" src/components/tables/WatchlistTab.tsx && grep -c "useMonitoring" src/components/tables/MonitoringTab.tsx && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/tables/StockTable.tsx exists and exports `StockTable`
    - StockTable.tsx contains `searchQuery` filter logic using `item.symbol.toLowerCase().includes`
    - StockTable.tsx contains `sortConfig` sorting logic with `localeCompare` for symbol field
    - StockTable.tsx contains `hidden md:block` for desktop table and `md:hidden` for mobile cards
    - StockTable.tsx contains 9 column headers including "전략 메모" (Strategic Note per D-09)
    - StockTable.tsx contains `SortHeader` usage for symbol, rs, ma50Dist, drawdown columns (4 sortable)
    - StockTable.tsx renders `ActionBadge` and `RevisionBadge` components
    - StockTable.tsx mobile card view shows Symbol + ActionBadge header, then 2x2 grid of Price/RS/Drawdown/MA50
    - src/components/tables/WatchlistTab.tsx imports `useWatchlist` and passes data to `StockTable`
    - src/components/tables/MonitoringTab.tsx imports `useMonitoring` and passes data to `StockTable`
    - WatchlistTab title contains "워치리스트"
    - MonitoringTab title contains "모니터링 리스트"
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Shared StockTable component created per D-02 with full interactivity: column sorting (D-07), symbol search (D-08), Strategic Note column (D-09), RS/Revision color coding (TAB2-06, TAB2-07), and mobile card view transformation (D-06). WatchlistTab and MonitoringTab wrappers wire up the correct hooks and pass metadata. Korean labels applied per DSGN-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> StockTable | StockTable receives data from TanStack Query hooks that fetch from `/api/watchlist` and `/api/monitoring`. Data is filtered through `isFullItem` type guard before rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Zustand searchQuery | accept | Client-only state for filtering local data. No server queries affected. Single-user app. |
| T-02-04 | Denial of Service | Sort on large dataset | accept | Dataset is fixed at 21 items max. No performance concern. |
| T-02-05 | Information Disclosure | Financial data in mobile cards | accept | Personal dashboard, no multi-user access. Data already visible in desktop view. |
</threat_model>

<verification>
1. All 7 component files exist in correct directories (ui/ and tables/)
2. `npx tsc --noEmit` passes
3. StockTable has exactly 9 columns including Strategic Note
4. Sort works for 4 fields (symbol, rs, ma50Dist, drawdown) with direction toggle
5. Search filters by symbol name (case-insensitive)
6. Mobile card view is hidden on md+ screens, desktop table hidden on small screens
7. Korean labels used for column headers and UI text
</verification>

<success_criteria>
StockTable shared component renders both Watchlist (12 tickers) and Monitoring (9 tickers) data with sortable columns, symbol search, RS/Revision color coding, Strategic Note column, and responsive mobile card view. WatchlistTab and MonitoringTab wrappers correctly wire TanStack Query hooks to the shared table.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-ui/02-02-SUMMARY.md`
</output>
