---
phase: 02-dashboard-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tables/StockTable.tsx
autonomous: true
gap_closure: true
requirements:
  - TAB2-04

must_haves:
  truths:
    - "User can click the drawdown column header to sort by drawdown"
  artifacts:
    - path: "src/components/tables/StockTable.tsx"
      provides: "Sortable drawdown column header"
      contains: "SortHeader field=\"drawdown\""
  key_links:
    - from: "src/components/tables/StockTable.tsx"
      to: "src/components/ui/SortHeader.tsx"
      via: "SortHeader component for drawdown field"
      pattern: "<SortHeader field=\"drawdown\""
---

<objective>
Fix the drawdown sort UI trigger in StockTable.tsx.

Purpose: TAB2-04 requires sorting by both RS strength and drawdown. The Zustand sort logic and StockTable sort implementation already handle the "drawdown" field correctly, but the column header is a plain `<th>` instead of a `<SortHeader>`, so the user has no way to trigger drawdown sort from the UI.

Output: StockTable.tsx with a clickable, sortable drawdown column header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-ui/02-VERIFICATION.md
@src/components/tables/StockTable.tsx
@src/components/ui/SortHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace plain th with SortHeader for drawdown column</name>
  <files>src/components/tables/StockTable.tsx</files>
  <read_first>
    - src/components/tables/StockTable.tsx (see current line 98 — the plain th for drawdown)
    - src/components/ui/SortHeader.tsx (confirm SortHeader accepts field="drawdown" and label prop)
  </read_first>
  <action>
In src/components/tables/StockTable.tsx, replace line 98:

```
<th className="px-6 py-4">고가 / 낙폭</th>
```

with:

```
<SortHeader field="drawdown" label="고가 / 낙폭" />
```

SortHeader is already imported in StockTable.tsx (used for "symbol", "rs", and "ma50Dist" columns). The "drawdown" field is already a valid SortField in the Zustand store's sortConfig type. No other changes needed — the sort logic in StockTable already handles numeric sorting for the drawdown field.

Do NOT modify any other lines. This is a single-line replacement.
  </action>
  <verify>
    <automated>grep -n 'SortHeader field="drawdown"' src/components/tables/StockTable.tsx && echo "PASS: drawdown SortHeader found" || echo "FAIL: drawdown SortHeader missing"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'field="drawdown"' src/components/tables/StockTable.tsx returns exactly one match containing `<SortHeader field="drawdown" label="고가 / 낙폭" />`
    - grep '<th.*고가 / 낙폭' src/components/tables/StockTable.tsx returns zero matches (plain th is gone)
    - npm run build completes without errors
  </acceptance_criteria>
  <done>The drawdown column header in StockTable.tsx is a clickable SortHeader that triggers sort by drawdown when clicked, completing TAB2-04 (RS and drawdown sort both exposed in UI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a UI-only change replacing a static element with an interactive one that was already used for other columns.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | T (Tampering) | SortHeader click handler | accept | Sort only reorders client-side display of already-fetched data; no server interaction or data mutation. Existing pattern used for 3 other columns. |
</threat_model>

<verification>
1. `grep -n 'SortHeader field="drawdown"' src/components/tables/StockTable.tsx` returns a match
2. `grep -c '<th.*고가 / 낙폭' src/components/tables/StockTable.tsx` returns 0
3. `npm run build` completes without errors
</verification>

<success_criteria>
- The "고가 / 낙폭" column header is a clickable SortHeader with ascending/descending toggle
- Clicking it sorts the table by drawdown value
- TAB2-04 requirement is fully satisfied (both RS and drawdown sort exposed in UI)
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-ui/02-04-SUMMARY.md`
</output>
