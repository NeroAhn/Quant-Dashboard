---
phase: 01-foundation-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/types/dashboard.ts
  - src/lib/constants.ts
  - src/lib/quant/types.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-05
  - ASIG-05
must_haves:
  truths:
    - "Next.js 15 프로젝트가 dev 서버로 정상 구동된다"
    - "모든 타입 정의가 export되어 후속 플랜에서 import 가능하다"
    - "21개 종목 + 6개 시장 심볼 상수가 정의되어 있다"
  artifacts:
    - path: "package.json"
      provides: "프로젝트 의존성"
      contains: "yahoo-finance2"
    - path: "src/types/dashboard.ts"
      provides: "공유 타입 정의"
      exports: ["WatchlistItem", "MarketDataItem", "ActionSignal", "Revision"]
    - path: "src/lib/constants.ts"
      provides: "종목 리스트, 심볼 매핑, 임계값 상수"
      exports: ["WATCHLIST_TICKERS", "MONITORING_TICKERS", "MARKET_SYMBOLS"]
    - path: "src/lib/quant/types.ts"
      provides: "퀀트 엔진 타입"
      exports: ["QuantMetrics", "ActionSignal", "Revision"]
  key_links:
    - from: "src/types/dashboard.ts"
      to: "src/lib/quant/types.ts"
      via: "re-export or import"
      pattern: "import.*from.*quant/types"
---

<objective>
Next.js 15 프로젝트를 초기화하고, 전체 Phase 1에서 사용할 타입 정의와 상수를 확립한다.

Purpose: 후속 플랜(퀀트 엔진, API 라우트, 클라이언트 훅)이 동일한 타입 계약과 상수를 기반으로 독립적으로 구현될 수 있도록 인터페이스를 선행 정의한다.
Output: 구동 가능한 Next.js 15 프로젝트 + 타입/상수 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 15 프로젝트 초기화 및 의존성 설치</name>
  <files>package.json, tsconfig.json, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-data-pipeline/01-RESEARCH.md (Installation 섹션, Node.js 버전 이슈)
    - CLAUDE.md (기술 스택 버전 정보)
  </read_first>
  <action>
1. Node.js 버전 확인: `node -v` 실행. v18 미만이면 `nvs add 20 && nvs use 20 && nvs link 20` 실행.
2. 현재 디렉토리에 package.json이 이미 존재하는지 확인:
   - package.json이 없으면: `npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` 직접 실행.
   - package.json이 이미 존재하면: 임시 디렉토리 `/tmp/next-init-temp`에 생성 후 필요 파일(package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, postcss.config.mjs)을 현재 디렉토리로 복사. 이후 `/tmp/next-init-temp` 삭제.
3. Core 의존성 설치:
   ```bash
   npm install yahoo-finance2@^3.14.0 @tanstack/react-query@^5.99.0 zustand@^5.0.12 lucide-react@^1.8.0
   ```
4. Dev 의존성 설치:
   ```bash
   npm install -D @tanstack/react-query-devtools@^5.99.0
   ```
5. `src/app/page.tsx`를 최소 플레이스홀더로 교체 (Phase 2에서 대시보드 UI 구현):
   ```tsx
   export default function Home() {
     return (
       <main className="min-h-screen bg-[#F8FAFC] p-8">
         <h1 className="text-2xl font-bold text-[#1E293B]">Alpha Engine Dashboard</h1>
         <p className="text-gray-500 mt-2">Phase 1: Data Pipeline 구축 중...</p>
       </main>
     );
   }
   ```
6. `npm run build`로 빌드 성공 확인.
7. git init 후 초기 커밋 생성.
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && node -v && echo "---NODE CHECK ABOVE---" && npm run build 2>&1 | tail -10 && echo "---BUILD CHECK ABOVE---"</automated>
  </verify>
  <acceptance_criteria>
    - Node.js v18+ 확인 (node -v 출력)
    - `npm run build` exits with code 0 (별도 확인)
    - package.json contains "yahoo-finance2"
    - package.json contains "@tanstack/react-query"
    - package.json contains "zustand"
    - package.json contains "lucide-react"
    - package.json contains "next" with version 15.x
    - src/app/page.tsx contains "Alpha Engine Dashboard"
    - src/app/layout.tsx exists
  </acceptance_criteria>
  <done>Next.js 15 프로젝트가 빌드 성공하고, 모든 Core/Dev 의존성이 설치됨</done>
</task>

<task type="auto">
  <name>Task 2: 타입 정의 및 상수 파일 생성</name>
  <files>src/types/dashboard.ts, src/lib/quant/types.ts, src/lib/constants.ts</files>
  <read_first>
    - .planning/PROJECT.md (종목 리스트, 퀀트 공식, 액션 시그널 정의)
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md (D-01, D-07, D-08, D-09 결정)
    - .planning/phases/01-foundation-data-pipeline/01-RESEARCH.md (Code Examples 섹션 - WATCHLIST_TICKERS, MONITORING_TICKERS, MARKET_SYMBOLS, 타입 정의)
  </read_first>
  <action>
1. `src/lib/quant/types.ts` 생성 — 퀀트 엔진 핵심 타입:
   ```typescript
   export type Revision = "UP" | "DOWN" | "NEUTRAL";
   export type ActionSignal = "Buy" | "Trim" | "Wait" | "Hold";

   export interface QuantMetrics {
     rs: number;           // Relative Strength (D-09: 1D 기준)
     ma50Dist: number;     // MA50 이격도 (%)
     drawdown: number;     // 52주 고가 대비 하락률 (%)
     revision: Revision;   // EPS Revision 방향
     action: ActionSignal; // 조건부 액션 시그널
   }

   // ASIG-05: 시그널 색상 코딩 매핑
   export const ACTION_SIGNAL_COLORS: Record<ActionSignal, string> = {
     Buy: "green",
     Trim: "red",
     Wait: "yellow",
     Hold: "gray",
   };
   ```

2. `src/types/dashboard.ts` 생성 — API 응답 및 UI 공유 타입:
   ```typescript
   import type { QuantMetrics, ActionSignal, Revision } from "@/lib/quant/types";

   export type { ActionSignal, Revision } from "@/lib/quant/types";

   export interface WatchlistItem extends QuantMetrics {
     symbol: string;
     price: number | null;
     change1D: number | null;     // 1D 변동률 (%)
     high52w: number | null;      // 52주 고가
     forwardPE: number | null;    // Forward P/E
     error?: string;              // D-02: 부분 실패 시 에러 메시지
   }

   export interface WatchlistResponse {
     data: (WatchlistItem | { symbol: string; error: string })[];
     timestamp: string;           // ISO 8601
   }

   export interface MarketDataItem {
     name: string;                // 표시명 (예: "S&P 500")
     symbol: string;              // Yahoo Finance 심볼
     price: number | null;
     change: number | null;       // 변동률 또는 텍스트 (Fear & Greed)
     changePercent: number | null;
     error?: string;
   }

   export interface FearGreedData {
     score: number;               // 0-100
     label: string;               // "Extreme Fear", "Fear", "Neutral", "Greed", "Extreme Greed"
     display: string;             // D-07: "42 (Fear)" 형태
   }

   export interface MarketDataResponse {
     data: MarketDataItem[];
     fearGreed: FearGreedData | null;
     timestamp: string;
   }
   ```

3. `src/lib/constants.ts` 생성 — 종목 리스트, 심볼 매핑, 임계값:
   ```typescript
   // PROJECT.md 기본 Watchlist 종목 (12개)
   export const WATCHLIST_TICKERS = [
     "AAPL", "GOOGL", "META", "MSFT", "NVDA", "TSLA",
     "PLTR", "IONQ", "OKLO", "OXY", "V", "UNH",
   ] as const;

   // PROJECT.md 기본 Monitoring 종목 (9개)
   export const MONITORING_TICKERS = [
     "MU", "AMZN", "NFLX", "CEG", "SO", "NEE", "BRK-B", "LLY", "JNJ",
   ] as const;

   // D-01: Market Data 심볼 매핑 (6개 지수/자산)
   export const MARKET_SYMBOLS = {
     "S&P 500": "^GSPC",
     "NASDAQ": "^IXIC",
     "BTC": "BTC-USD",
     "GOLD": "GC=F",
     "10Y Yield": "^TNX",
     "DXY": "DX-Y.NYB",
   } as const;

   export type MarketName = keyof typeof MARKET_SYMBOLS;

   // 퀀트 엔진 임계값 (QENG-04: 부동소수점 비교용)
   export const QUANT_THRESHOLDS = {
     RS_BUY: 110,
     RS_TRIM: 130,
     RS_WAIT: 90,
     MA50_BUY: 5,
     MA50_TRIM: 12,
     EPSILON: 1e-10,
   } as const;

   // Macro 경고 임계값 (TAB1-04, Phase 2에서 UI 사용)
   export const MACRO_THRESHOLDS = {
     YIELD_10Y_WARNING: 4.5,
     DXY_WARNING: 106.0,
     FEAR_GREED_WARNING: 30,
   } as const;

   // Fear & Greed 라벨 매핑
   export const FEAR_GREED_LABELS: Record<string, [number, number]> = {
     "Extreme Fear": [0, 24],
     "Fear": [25, 44],
     "Neutral": [45, 55],
     "Greed": [56, 74],
     "Extreme Greed": [75, 100],
   } as const;
   ```

4. TypeScript 컴파일 에러가 없는지 `npx tsc --noEmit` 확인.
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/quant/types.ts contains "export type Revision ="
    - src/lib/quant/types.ts contains "export type ActionSignal ="
    - src/lib/quant/types.ts contains "ACTION_SIGNAL_COLORS"
    - src/types/dashboard.ts contains "export interface WatchlistItem"
    - src/types/dashboard.ts contains "export interface MarketDataResponse"
    - src/types/dashboard.ts contains "export interface FearGreedData"
    - src/lib/constants.ts contains "WATCHLIST_TICKERS"
    - src/lib/constants.ts contains "MONITORING_TICKERS"
    - src/lib/constants.ts contains "MARKET_SYMBOLS"
    - src/lib/constants.ts contains "QUANT_THRESHOLDS"
    - src/lib/constants.ts contains "BRK-B" (not BRK.B)
    - WATCHLIST_TICKERS array has exactly 12 elements
    - MONITORING_TICKERS array has exactly 9 elements
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>모든 타입과 상수가 정의되고 TypeScript 컴파일 에러 없음. 후속 플랜에서 이 파일들을 import하여 사용 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 | Plan 01은 프로젝트 초기화 및 타입 정의만 수행. 외부 입력/네트워크 통신 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | package.json dependencies | mitigate | npm install 시 ^(caret) 범위 지정으로 minor 업데이트만 허용. package-lock.json 커밋 |
| T-01-02 | I (Information Disclosure) | .env.local | accept | Phase 1에서는 API 키 불필요 (yahoo-finance2는 키 없이 작동). .gitignore에 .env* 포함 확인 |
</threat_model>

<verification>
1. `npm run build` 성공
2. `npx tsc --noEmit` 성공
3. `node -v`가 v18+ 출력
4. package.json에 모든 core 의존성 존재
5. src/types/dashboard.ts, src/lib/quant/types.ts, src/lib/constants.ts 파일 존재 및 export 확인
</verification>

<success_criteria>
- Next.js 15 프로젝트가 `npm run dev`와 `npm run build` 모두 성공
- 모든 core 의존성 (yahoo-finance2, @tanstack/react-query, zustand, lucide-react) 설치됨
- 타입 정의 파일이 TypeScript 에러 없이 컴파일됨
- 21개 종목 상수와 6개 시장 심볼이 정확히 정의됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-01-SUMMARY.md`
</output>
