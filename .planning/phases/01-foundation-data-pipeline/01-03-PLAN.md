---
phase: 01-foundation-data-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/fear-greed/client.ts
  - src/app/api/market-data/route.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "/api/market-data가 6개 시장 지표(S&P 500, NASDAQ, BTC, GOLD, 10Y Yield, DXY)를 반환한다"
    - "Fear & Greed 데이터가 숫자 + 텍스트 라벨 형태로 포함된다 (D-07)"
    - "Fear & Greed API 실패 시 fearGreed가 null로 반환된다"
    - "개별 시장 지표 실패 시 해당 항목만 error 표시 (D-02)"
  artifacts:
    - path: "src/lib/fear-greed/client.ts"
      provides: "Fear & Greed Index API 클라이언트"
      exports: ["fetchFearGreedIndex"]
    - path: "src/app/api/market-data/route.ts"
      provides: "GET /api/market-data 엔드포인트"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/market-data/route.ts"
      to: "yahoo-finance2"
      via: "yahooFinance.quote() for market symbols"
      pattern: "yahooFinance\\.quote"
    - from: "src/app/api/market-data/route.ts"
      to: "src/lib/fear-greed/client.ts"
      via: "fetchFearGreedIndex import"
      pattern: "import.*fetchFearGreedIndex"
---

<objective>
Market Data API 라우트를 구축한다. 6개 시장 지표(S&P 500, NASDAQ, BTC, GOLD, 10Y Yield, DXY)와 Fear & Greed Index를 단일 엔드포인트로 제공한다.

Purpose: 대시보드 Strategic Overview 탭의 Market Snapshot 카드와 Macro Threshold Warnings에 필요한 데이터 소스를 완성한다.
Output: 작동하는 /api/market-data 엔드포인트 + Fear & Greed 클라이언트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-RESEARCH.md
@CLAUDE.md

<interfaces>
From src/lib/constants.ts:
```typescript
export const MARKET_SYMBOLS = {
  "S&P 500": "^GSPC", "NASDAQ": "^IXIC", "BTC": "BTC-USD",
  "GOLD": "GC=F", "10Y Yield": "^TNX", "DXY": "DX-Y.NYB",
} as const;
export type MarketName = keyof typeof MARKET_SYMBOLS;
```

From src/types/dashboard.ts:
```typescript
export interface MarketDataItem {
  name: string; symbol: string; price: number|null;
  change: number|null; changePercent: number|null; error?: string;
}
export interface FearGreedData {
  score: number; label: string; display: string; // "42 (Fear)"
}
export interface MarketDataResponse {
  data: MarketDataItem[]; fearGreed: FearGreedData|null; timestamp: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fear & Greed Index 클라이언트</name>
  <files>src/lib/fear-greed/client.ts</files>
  <read_first>
    - src/types/dashboard.ts (FearGreedData 인터페이스)
    - .planning/phases/01-foundation-data-pipeline/01-RESEARCH.md (Fear & Greed Index 데이터 소싱 섹션, Pitfall 6)
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md (D-05: 대체 무료 API, D-07: 숫자+텍스트 표시)
  </read_first>
  <action>
`src/lib/fear-greed/client.ts` 생성:

```typescript
import type { FearGreedData } from "@/types/dashboard";

const FEAR_GREED_API_URL = "https://feargreedchart.com/api/?action=all";

interface FearGreedAPIResponse {
  score?: number;
  rating?: string;
  // feargreedchart.com 실제 응답 구조에 맞게 조정 필요
}

function getLabel(score: number): string {
  if (score <= 24) return "Extreme Fear";
  if (score <= 44) return "Fear";
  if (score <= 55) return "Neutral";
  if (score <= 74) return "Greed";
  return "Extreme Greed";
}

export async function fetchFearGreedIndex(): Promise<FearGreedData | null> {
  try {
    const response = await fetch(FEAR_GREED_API_URL, {
      signal: AbortSignal.timeout(10000), // 10초 타임아웃
      headers: { "User-Agent": "AlphaEngine/1.0" },
    });

    if (!response.ok) {
      console.error(`Fear & Greed API returned ${response.status}`);
      return null;
    }

    const data: FearGreedAPIResponse = await response.json();

    if (data.score === undefined || data.score === null) {
      console.error("Fear & Greed API: score field missing");
      return null;
    }

    const score = Math.round(data.score);
    const label = data.rating || getLabel(score);

    return {
      score,
      label,
      display: `${score} (${label})`, // D-07: "42 (Fear)" 형태
    };
  } catch (error) {
    // Pitfall 6: 서드파티 API 다운 시 null 반환
    console.error("Fear & Greed fetch failed:", error);
    return null;
  }
}
```

Note: feargreedchart.com API의 실제 응답 구조는 첫 호출 시 로깅하여 확인 후 인터페이스를 조정해야 할 수 있음 (01-RESEARCH.md A2 가정).
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/fear-greed/client.ts contains "export async function fetchFearGreedIndex("
    - src/lib/fear-greed/client.ts contains "feargreedchart.com" (D-05)
    - src/lib/fear-greed/client.ts contains "return null" at least twice (API 실패 + score 누락)
    - src/lib/fear-greed/client.ts contains "display:" with template literal showing score + label (D-07)
    - src/lib/fear-greed/client.ts contains "AbortSignal.timeout" (타임아웃 보호)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Fear & Greed Index 클라이언트가 feargreedchart.com API에서 데이터를 가져오고, 실패 시 null을 반환하며, D-07 형식("42 (Fear)")으로 display 문자열을 생성함.</done>
</task>

<task type="auto">
  <name>Task 2: Market Data API 라우트 (/api/market-data)</name>
  <files>src/app/api/market-data/route.ts</files>
  <read_first>
    - src/lib/fear-greed/client.ts (Task 1에서 생성)
    - src/lib/constants.ts (MARKET_SYMBOLS)
    - src/types/dashboard.ts (MarketDataItem, MarketDataResponse)
    - .planning/phases/01-foundation-data-pipeline/01-RESEARCH.md (Code Examples - MARKET_SYMBOLS, Pitfall 2 Rate Limiting)
  </read_first>
  <action>
`src/app/api/market-data/route.ts` 생성 (D-01, D-04):

**IMPORTANT:** 이 라우트는 Plan 01-02와 병렬 실행 (둘 다 wave 2)이므로 Plan 01-02의 `src/lib/yahoo-finance/client.ts`에 의존하지 않는다. yahoo-finance2를 직접 import하고 자체적으로 `setGlobalConfig()`를 호출한다.

```typescript
import { NextResponse } from "next/server";
import yahooFinance from "yahoo-finance2";
import { MARKET_SYMBOLS } from "@/lib/constants";
import { fetchFearGreedIndex } from "@/lib/fear-greed/client";
import type { MarketDataItem, MarketDataResponse } from "@/types/dashboard";

// yahoo-finance2 validation 경고 억제 — 이 라우트가 독립적으로 실행되므로 자체 설정
yahooFinance.setGlobalConfig({ validation: { logErrors: false } });

export async function GET() {
  try {
    // D-04: Fear & Greed와 시장 지표를 병렬로 fetch
    const [marketResults, fearGreed] = await Promise.all([
      fetchMarketSymbols(),
      fetchFearGreedIndex(),
    ]);

    const response: MarketDataResponse = {
      data: marketResults,
      fearGreed,
      timestamp: new Date().toISOString(),
    };

    return NextResponse.json(response);
  } catch (error) {
    return NextResponse.json(
      { error: "Failed to fetch market data" },
      { status: 500 }
    );
  }
}

async function fetchMarketSymbols(): Promise<MarketDataItem[]> {
  const entries = Object.entries(MARKET_SYMBOLS) as [string, string][];

  // Promise.allSettled로 개별 실패 허용 (D-02)
  const results = await Promise.allSettled(
    entries.map(async ([name, symbol]) => {
      const quote = await yahooFinance.quote(symbol);
      return {
        name,
        symbol,
        price: quote.regularMarketPrice ?? null,
        change: quote.regularMarketChange ?? null,
        changePercent: quote.regularMarketChangePercent ?? null,
      } satisfies MarketDataItem;
    })
  );

  return results.map((result, i) => {
    if (result.status === "fulfilled") return result.value;
    return {
      name: entries[i][0],
      symbol: entries[i][1],
      price: null,
      change: null,
      changePercent: null,
      error: result.reason?.message ?? "Unknown error",
    } satisfies MarketDataItem;
  });
}
```

이 라우트는 Plan 01-02의 `src/lib/yahoo-finance/client.ts`가 존재하지 않아도 독립적으로 작동한다. Plan 01-02 완료 후 공유 client.ts로 리팩토링 가능하지만 기능적으로는 불필요.
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/market-data/route.ts contains "export async function GET("
    - src/app/api/market-data/route.ts contains "MARKET_SYMBOLS"
    - src/app/api/market-data/route.ts contains "fetchFearGreedIndex"
    - src/app/api/market-data/route.ts contains "Promise.allSettled" (D-02)
    - src/app/api/market-data/route.ts contains "Promise.all" (D-04 병렬 fetch)
    - src/app/api/market-data/route.ts contains "timestamp"
    - src/app/api/market-data/route.ts contains "fearGreed"
    - src/app/api/market-data/route.ts contains "yahooFinance.setGlobalConfig" (독립적 글로벌 설정)
    - src/app/api/market-data/route.ts does NOT import from "src/lib/yahoo-finance/client"
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>/api/market-data가 6개 시장 지표 + Fear & Greed 데이터를 JSON으로 반환. 개별 지표 실패 시 해당 항목만 error 표시. Fear & Greed 실패 시 null. Plan 01-02와 독립적으로 실행 가능 (yahoo-finance2 직접 import + 자체 setGlobalConfig).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → feargreedchart.com | 외부 서드파티 무료 API. SLA 없음, 응답 구조 변경 가능 |
| Server → Yahoo Finance API | 외부 API. Rate limiting, 심볼 유효성 |
| Client → /api/market-data | Public GET endpoint, 파라미터 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | D (Denial of Service) | feargreedchart.com API | mitigate | AbortSignal.timeout(10000)으로 10초 타임아웃. 실패 시 null 반환, 나머지 시장 데이터는 정상 제공 |
| T-03-02 | T (Tampering) | Fear & Greed API 응답 | mitigate | score 값 범위 검증 (0-100), 타입 체크. 비정상 데이터 시 null 반환 |
| T-03-03 | I (Information Disclosure) | /api/market-data 에러 응답 | mitigate | 내부 에러 상세를 console.error로만 로깅, 클라이언트에는 generic message 반환 |
| T-03-04 | D (Denial of Service) | Yahoo Finance rate limiting | accept | 6개 심볼 동시 호출은 Yahoo Finance 무료 티어 한도 내. 5분 갱신 주기로 추가 보호 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript 에러 없음
2. `curl http://localhost:3000/api/market-data` — 6개 시장 지표 + fearGreed 필드 포함 JSON 반환
3. 응답 JSON의 data 배열에 6개 항목 존재 확인
4. fearGreed.display 형식이 "42 (Fear)" 패턴인지 확인
</verification>

<success_criteria>
- /api/market-data가 S&P 500, NASDAQ, BTC, GOLD, 10Y Yield, DXY 6개 지표를 반환
- Fear & Greed Index가 숫자 + 텍스트 라벨 형태(D-07)로 포함
- 개별 지표 실패 시 부분 반환 작동(D-02)
- Fear & Greed API 실패 시 fearGreed가 null (나머지 데이터 정상)
- Plan 01-02와 병렬 실행 가능 (yahoo-finance2 직접 import, 공유 client.ts 불필요)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-03-SUMMARY.md`
</output>
