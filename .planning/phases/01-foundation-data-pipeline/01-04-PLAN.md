---
phase: 01-foundation-data-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/app/providers.tsx
  - src/app/layout.tsx
  - src/hooks/use-watchlist.ts
  - src/hooks/use-monitoring.ts
  - src/hooks/use-market-data.ts
  - src/store/dashboard.ts
  - src/components/skeleton.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - DATA-02
  - DATA-06
  - DATA-07
  - DATA-08
must_haves:
  truths:
    - "TanStack Query가 5분 간격으로 자동 갱신한다 (장 운영시간)"
    - "장 마감 후 30분 간격으로 갱신 주기가 변경된다 (D-12)"
    - "데이터 갱신 시각(dataUpdatedAt)이 표시된다 (DATA-08)"
    - "로딩 중 Skeleton placeholder UI가 화면에 표시된다 (DATA-07)"
    - "Zustand 스토어가 activeTab 상태를 관리한다"
    - "API 장애 시 stale 데이터가 유지되지 않고 에러 상태로 전환된다 (D-11)"
  artifacts:
    - path: "src/app/providers.tsx"
      provides: "QueryClientProvider 래퍼"
      exports: ["Providers"]
    - path: "src/hooks/use-watchlist.ts"
      provides: "Watchlist 데이터 훅"
      exports: ["useWatchlist"]
    - path: "src/hooks/use-monitoring.ts"
      provides: "Monitoring 데이터 훅"
      exports: ["useMonitoring"]
    - path: "src/hooks/use-market-data.ts"
      provides: "Market Data 훅"
      exports: ["useMarketData"]
    - path: "src/store/dashboard.ts"
      provides: "Zustand 대시보드 스토어"
      exports: ["useDashboardStore"]
    - path: "src/components/skeleton.tsx"
      provides: "Skeleton placeholder 컴포넌트"
      exports: ["TableSkeleton", "MarketCardSkeleton"]
  key_links:
    - from: "src/hooks/use-watchlist.ts"
      to: "/api/watchlist"
      via: "fetch in useQuery queryFn"
      pattern: "fetch.*api/watchlist"
    - from: "src/hooks/use-market-data.ts"
      to: "/api/market-data"
      via: "fetch in useQuery queryFn"
      pattern: "fetch.*api/market-data"
    - from: "src/app/layout.tsx"
      to: "src/app/providers.tsx"
      via: "Providers component wrapping children"
      pattern: "<Providers>"
    - from: "src/app/page.tsx"
      to: "src/components/skeleton.tsx"
      via: "Skeleton components for loading state"
      pattern: "import.*Skeleton.*from.*components/skeleton"
---

<objective>
TanStack Query 클라이언트 훅, Zustand 스토어, QueryClientProvider를 구축하여 데이터 자동 갱신 파이프라인을 완성하고, Skeleton UI 로딩 상태와 기본 데이터 표시 페이지로 전체 파이프라인이 작동함을 검증한다.

Purpose: Phase 1의 마지막 조각으로, 서버 API(Plan 02/03)와 클라이언트(Phase 2 UI)를 연결하는 데이터 훅 계층을 완성한다. 이 훅들은 Phase 2의 모든 UI 컴포넌트에서 사용된다.
Output: 자동 갱신 훅 3개 + Zustand 스토어 + Skeleton 컴포넌트 + 작동 검증용 페이지
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-RESEARCH.md
@CLAUDE.md

@.planning/phases/01-foundation-data-pipeline/01-02-SUMMARY.md
@.planning/phases/01-foundation-data-pipeline/01-03-SUMMARY.md

<interfaces>
From src/types/dashboard.ts:
```typescript
export interface WatchlistResponse { data: (WatchlistItem | { symbol: string; error: string })[]; timestamp: string; }
export interface MarketDataResponse { data: MarketDataItem[]; fearGreed: FearGreedData|null; timestamp: string; }
```

From src/lib/quant/types.ts:
```typescript
export type ActionSignal = "Buy" | "Trim" | "Wait" | "Hold";
export const ACTION_SIGNAL_COLORS: Record<ActionSignal, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QueryClientProvider + Zustand 스토어 + TanStack Query 훅</name>
  <files>src/app/providers.tsx, src/app/layout.tsx, src/hooks/use-watchlist.ts, src/hooks/use-monitoring.ts, src/hooks/use-market-data.ts, src/store/dashboard.ts</files>
  <read_first>
    - src/app/layout.tsx (현재 레이아웃 구조)
    - src/types/dashboard.ts (WatchlistResponse, MarketDataResponse 타입)
    - .planning/phases/01-foundation-data-pipeline/01-RESEARCH.md (Pattern 4: TanStack Query with Conditional Polling, QueryClient 설정, Pitfall 5)
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md (D-11: stale 데이터 미유지, D-12: 조건부 갱신 주기)
  </read_first>
  <action>
1. `src/app/providers.tsx` 생성 — QueryClientProvider 설정 (D-11: stale 데이터 미유지):
   ```typescript
   "use client";
   import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
   import { ReactQueryDevtools } from "@tanstack/react-query-devtools";
   import { useState, type ReactNode } from "react";

   export function Providers({ children }: { children: ReactNode }) {
     const [queryClient] = useState(() => new QueryClient({
       defaultOptions: {
         queries: {
           retry: 2,
           retryDelay: (attemptIndex) => Math.min(1000 * 2 ** attemptIndex, 10000),
           staleTime: 0,             // D-11: 에러 시 즉시 stale 처리, 캐시된 stale 데이터 사용하지 않음
           gcTime: 5 * 60 * 1000,    // D-11: refetchInterval과 동일 (5분). 실패한 쿼리 캐시를 다음 갱신 시점에 제거
         },
       },
     }));

     return (
       <QueryClientProvider client={queryClient}>
         {children}
         <ReactQueryDevtools initialIsOpen={false} />
       </QueryClientProvider>
     );
   }
   ```

2. `src/app/layout.tsx` 수정 — Providers로 children 래핑:
   기존 layout.tsx를 읽고, `<body>` 내부의 `{children}`을 `<Providers>{children}</Providers>`로 감쌈.
   상단에 `import { Providers } from "./providers";` 추가.

3. `src/hooks/use-watchlist.ts` 생성 (DATA-02, DATA-06, D-12):
   ```typescript
   import { useQuery } from "@tanstack/react-query";
   import type { WatchlistResponse } from "@/types/dashboard";

   function getStockRefetchInterval(): number {
     const now = new Date();
     const et = new Date(now.toLocaleString("en-US", { timeZone: "America/New_York" }));
     const hour = et.getHours();
     const minute = et.getMinutes();
     const day = et.getDay();

     // 주말: 30분
     if (day === 0 || day === 6) return 30 * 60 * 1000;

     // 장 운영시간 (ET 9:30-16:00): 5분
     const marketOpen = hour > 9 || (hour === 9 && minute >= 30);
     const marketClose = hour < 16;
     if (marketOpen && marketClose) return 5 * 60 * 1000;

     // 장 마감 후: 30분
     return 30 * 60 * 1000;
   }

   export function useWatchlist() {
     return useQuery<WatchlistResponse>({
       queryKey: ["watchlist"],
       queryFn: async () => {
         const res = await fetch("/api/watchlist");
         if (!res.ok) throw new Error(`Watchlist fetch failed: ${res.status}`);
         return res.json();
       },
       refetchInterval: getStockRefetchInterval,
       refetchIntervalInBackground: true,
       staleTime: 0, // D-11: stale 데이터 유지하지 않음
     });
   }
   ```

4. `src/hooks/use-monitoring.ts` 생성 — use-watchlist와 동일 구조, queryKey: ["monitoring"], endpoint: "/api/monitoring". staleTime: 0 동일 적용 (D-11).

5. `src/hooks/use-market-data.ts` 생성 (D-12: market-data는 24시간 자산 포함이므로 항상 5분):
   ```typescript
   import { useQuery } from "@tanstack/react-query";
   import type { MarketDataResponse } from "@/types/dashboard";

   export function useMarketData() {
     return useQuery<MarketDataResponse>({
       queryKey: ["market-data"],
       queryFn: async () => {
         const res = await fetch("/api/market-data");
         if (!res.ok) throw new Error(`Market data fetch failed: ${res.status}`);
         return res.json();
       },
       refetchInterval: 5 * 60 * 1000, // D-12: BTC/Gold/DXY 등 24시간 자산 항상 5분
       refetchIntervalInBackground: true,
       staleTime: 0, // D-11: stale 데이터 유지하지 않음
     });
   }
   ```

6. `src/store/dashboard.ts` 생성 — Zustand 스토어:
   ```typescript
   import { create } from "zustand";
   import { persist } from "zustand/middleware";

   type Tab = "overview" | "watchlist" | "monitoring";
   type SortField = "rs" | "drawdown" | "ma50Dist" | "symbol";
   type SortDirection = "asc" | "desc";

   interface DashboardState {
     activeTab: Tab;
     setActiveTab: (tab: Tab) => void;
     searchQuery: string;
     setSearchQuery: (query: string) => void;
     sortConfig: { field: SortField; direction: SortDirection };
     setSortConfig: (field: SortField) => void;
   }

   export const useDashboardStore = create<DashboardState>()(
     persist(
       (set, get) => ({
         activeTab: "overview",
         setActiveTab: (tab) => set({ activeTab: tab }),
         searchQuery: "",
         setSearchQuery: (query) => set({ searchQuery: query }),
         sortConfig: { field: "rs", direction: "desc" },
         setSortConfig: (field) => set((state) => ({
           sortConfig: {
             field,
             direction: state.sortConfig.field === field && state.sortConfig.direction === "desc"
               ? "asc"
               : "desc",
           },
         })),
       }),
       { name: "dashboard-preferences" }
     )
   );
   ```
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/providers.tsx contains "QueryClientProvider"
    - src/app/providers.tsx contains '"use client"'
    - src/app/providers.tsx contains "ReactQueryDevtools"
    - src/app/providers.tsx contains "staleTime: 0" (D-11)
    - src/app/providers.tsx contains "gcTime: 5 * 60 * 1000" (D-11: refetchInterval과 동일)
    - src/app/layout.tsx contains "<Providers>"
    - src/hooks/use-watchlist.ts contains "export function useWatchlist("
    - src/hooks/use-watchlist.ts contains 'refetchInterval: getStockRefetchInterval'
    - src/hooks/use-watchlist.ts contains "America/New_York" (D-12 ET 시간대)
    - src/hooks/use-watchlist.ts contains "5 * 60 * 1000" (5분 장중 갱신)
    - src/hooks/use-watchlist.ts contains "30 * 60 * 1000" (30분 장마감 후)
    - src/hooks/use-watchlist.ts contains "staleTime: 0" (D-11)
    - src/hooks/use-monitoring.ts contains "export function useMonitoring("
    - src/hooks/use-market-data.ts contains "export function useMarketData("
    - src/hooks/use-market-data.ts contains "5 * 60 * 1000" (D-12: 항상 5분)
    - src/hooks/use-market-data.ts contains "staleTime: 0" (D-11)
    - src/store/dashboard.ts contains "export const useDashboardStore"
    - src/store/dashboard.ts contains "persist" (Zustand persist 미들웨어)
    - src/store/dashboard.ts contains "activeTab"
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>TanStack Query 훅 3개(watchlist, monitoring, market-data)가 조건부 갱신 주기를 구현하고, D-11에 따라 staleTime: 0 + gcTime: 5분으로 설정. Zustand 스토어가 탭/검색/정렬 상태를 관리. QueryClientProvider가 layout에 통합됨.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton UI 컴포넌트 + 데이터 파이프라인 통합 검증 페이지</name>
  <files>src/components/skeleton.tsx, src/app/page.tsx</files>
  <read_first>
    - src/hooks/use-watchlist.ts (Task 1에서 생성)
    - src/hooks/use-market-data.ts (Task 1에서 생성)
    - src/types/dashboard.ts (응답 타입)
    - src/lib/quant/types.ts (ACTION_SIGNAL_COLORS)
  </read_first>
  <action>
1. `src/components/skeleton.tsx` 생성 — DATA-07 Skeleton placeholder UI:
   ```tsx
   "use client";

   /** 애니메이션 펄스 기본 Skeleton 블록 */
   function SkeletonBlock({ className = "" }: { className?: string }) {
     return (
       <div className={`animate-pulse bg-gray-200 rounded ${className}`} />
     );
   }

   /** Market Data 카드 Skeleton (6개 지표 + Fear & Greed) */
   export function MarketCardSkeleton() {
     return (
       <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
         {Array.from({ length: 7 }).map((_, i) => (
           <div key={i} className="border p-3 rounded">
             <SkeletonBlock className="h-4 w-20 mb-2" />
             <SkeletonBlock className="h-6 w-16 mb-1" />
             <SkeletonBlock className="h-4 w-12" />
           </div>
         ))}
       </div>
     );
   }

   /** Watchlist/Monitoring 테이블 Skeleton */
   export function TableSkeleton({ rows = 12 }: { rows?: number }) {
     return (
       <table className="w-full text-sm border-collapse">
         <thead>
           <tr className="border-b text-left">
             {["Symbol", "Price", "1D%", "RS", "MA50 Dist", "Drawdown", "Revision", "Action"].map((h) => (
               <th key={h} className="p-2 text-gray-400">{h}</th>
             ))}
           </tr>
         </thead>
         <tbody>
           {Array.from({ length: rows }).map((_, i) => (
             <tr key={i} className="border-b">
               <td className="p-2"><SkeletonBlock className="h-4 w-12" /></td>
               <td className="p-2"><SkeletonBlock className="h-4 w-16" /></td>
               <td className="p-2"><SkeletonBlock className="h-4 w-12" /></td>
               <td className="p-2"><SkeletonBlock className="h-4 w-10" /></td>
               <td className="p-2"><SkeletonBlock className="h-4 w-14" /></td>
               <td className="p-2"><SkeletonBlock className="h-4 w-14" /></td>
               <td className="p-2"><SkeletonBlock className="h-4 w-16" /></td>
               <td className="p-2"><SkeletonBlock className="h-4 w-10" /></td>
             </tr>
           ))}
         </tbody>
       </table>
     );
   }
   ```

2. `src/app/page.tsx`를 데이터 파이프라인 검증용 페이지로 교체. Phase 2에서 본격 UI로 대체될 예정이지만, Phase 1 Success Criteria 검증을 위해 모든 데이터가 화면에 표시되어야 함. DATA-07에 따라 로딩 시 Skeleton UI 표시:

   ```tsx
   "use client";

   import { useWatchlist } from "@/hooks/use-watchlist";
   import { useMonitoring } from "@/hooks/use-monitoring";
   import { useMarketData } from "@/hooks/use-market-data";
   import { MarketCardSkeleton, TableSkeleton } from "@/components/skeleton";

   export default function Home() {
     const watchlist = useWatchlist();
     const monitoring = useMonitoring();
     const marketData = useMarketData();

     return (
       <main className="min-h-screen bg-[#F8FAFC] p-8 font-mono">
         <h1 className="text-2xl font-bold text-[#1E293B] mb-6">
           Alpha Engine — Data Pipeline Verification
         </h1>

         {/* DATA-08: 데이터 갱신 시각 표시 */}
         <div className="mb-8 text-sm text-gray-500">
           {watchlist.dataUpdatedAt && (
             <p>Watchlist 갱신: {new Date(watchlist.dataUpdatedAt).toLocaleTimeString("ko-KR")}</p>
           )}
           {marketData.dataUpdatedAt && (
             <p>Market Data 갱신: {new Date(marketData.dataUpdatedAt).toLocaleTimeString("ko-KR")}</p>
           )}
         </div>

         {/* Market Data 섹션 */}
         <section className="mb-8">
           <h2 className="text-lg font-bold mb-2">Market Data</h2>
           {/* DATA-07: Skeleton UI during initial load */}
           {marketData.isPending && <MarketCardSkeleton />}
           {marketData.error && <p className="text-red-500">에러: {marketData.error.message}</p>}
           {marketData.data && (
             <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
               {marketData.data.data.map((item) => (
                 <div key={item.symbol} className="border p-3 rounded">
                   <p className="font-bold">{item.name}</p>
                   <p>{item.price ?? "N/A"}</p>
                   <p className={item.changePercent && item.changePercent >= 0 ? "text-green-600" : "text-red-600"}>
                     {item.changePercent != null ? `${item.changePercent.toFixed(2)}%` : "N/A"}
                   </p>
                   {item.error && <p className="text-red-400 text-xs">{item.error}</p>}
                 </div>
               ))}
               {marketData.data.fearGreed && (
                 <div className="border p-3 rounded">
                   <p className="font-bold">Fear & Greed</p>
                   <p>{marketData.data.fearGreed.display}</p>
                 </div>
               )}
             </div>
           )}
         </section>

         {/* Watchlist 섹션 */}
         <section className="mb-8">
           <h2 className="text-lg font-bold mb-2">Watchlist (12)</h2>
           {/* DATA-07: Skeleton UI during initial load */}
           {watchlist.isPending && <TableSkeleton rows={12} />}
           {watchlist.error && <p className="text-red-500">에러: {watchlist.error.message}</p>}
           {watchlist.data && (
             <table className="w-full text-sm border-collapse">
               <thead>
                 <tr className="border-b text-left">
                   <th className="p-2">Symbol</th>
                   <th className="p-2">Price</th>
                   <th className="p-2">1D%</th>
                   <th className="p-2">RS</th>
                   <th className="p-2">MA50 Dist</th>
                   <th className="p-2">Drawdown</th>
                   <th className="p-2">Revision</th>
                   <th className="p-2">Action</th>
                 </tr>
               </thead>
               <tbody>
                 {watchlist.data.data.map((item) => (
                   <tr key={item.symbol} className="border-b">
                     <td className="p-2 font-bold">{item.symbol}</td>
                     {"error" in item && !("price" in item) ? (
                       <td colSpan={7} className="p-2 text-red-400">{item.error}</td>
                     ) : (
                       <>
                         <td className="p-2">{"price" in item ? item.price : "N/A"}</td>
                         <td className="p-2">{"change1D" in item && item.change1D != null ? `${item.change1D.toFixed(2)}%` : "N/A"}</td>
                         <td className="p-2">{"rs" in item ? item.rs.toFixed(1) : "N/A"}</td>
                         <td className="p-2">{"ma50Dist" in item ? `${item.ma50Dist.toFixed(2)}%` : "N/A"}</td>
                         <td className="p-2">{"drawdown" in item ? `${item.drawdown.toFixed(2)}%` : "N/A"}</td>
                         <td className="p-2">{"revision" in item ? item.revision : "N/A"}</td>
                         <td className="p-2 font-bold">{"action" in item ? item.action : "N/A"}</td>
                       </>
                     )}
                   </tr>
                 ))}
               </tbody>
             </table>
           )}
         </section>

         {/* Monitoring 섹션 */}
         <section>
           <h2 className="text-lg font-bold mb-2">Monitoring (9)</h2>
           {/* DATA-07: Skeleton UI during initial load */}
           {monitoring.isPending && <TableSkeleton rows={9} />}
           {monitoring.error && <p className="text-red-500">에러: {monitoring.error.message}</p>}
           {monitoring.data && (
             <p className="text-sm text-gray-600">
               {monitoring.data.data.length}개 종목 로드됨 — 
               타임스탬프: {monitoring.data.timestamp}
             </p>
           )}
         </section>
       </main>
     );
   }
   ```

   이 페이지는 Phase 1 Success Criteria를 직접 검증할 수 있게 함:
   - SC1: Watchlist 테이블에 12개 종목 + 퀀트 메트릭 + 시그널 표시
   - SC2: Market Data에 7개 시장 지표 (6개 지수 + Fear & Greed) 표시
   - SC3: Action 컬럼에 Buy/Hold/Wait/Trim 표시
   - SC4: Skeleton placeholder UI 표시 + dataUpdatedAt 타임스탬프 표시 + 자동 갱신
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/skeleton.tsx contains "export function MarketCardSkeleton"
    - src/components/skeleton.tsx contains "export function TableSkeleton"
    - src/components/skeleton.tsx contains "animate-pulse" (Tailwind 펄스 애니메이션)
    - src/app/page.tsx contains '"use client"'
    - src/app/page.tsx contains "useWatchlist"
    - src/app/page.tsx contains "useMonitoring"
    - src/app/page.tsx contains "useMarketData"
    - src/app/page.tsx contains "MarketCardSkeleton" (DATA-07 import)
    - src/app/page.tsx contains "TableSkeleton" (DATA-07 import)
    - src/app/page.tsx contains "isPending && <MarketCardSkeleton" (DATA-07 skeleton 표시 조건)
    - src/app/page.tsx contains "isPending && <TableSkeleton" (DATA-07 skeleton 표시 조건)
    - src/app/page.tsx contains "dataUpdatedAt" (DATA-08)
    - src/app/page.tsx contains "fearGreed" (Fear & Greed 표시)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Skeleton UI 컴포넌트(MarketCardSkeleton, TableSkeleton)가 구현되어 초기 로딩 시 animate-pulse placeholder가 표시됨(DATA-07). 데이터 파이프라인 전체 검증 가능. dev 서버에서 12개 watchlist 종목, 9개 monitoring 종목, 6개 시장 지표, Fear & Greed가 화면에 표시되고 자동 갱신됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → /api/* | 클라이언트 훅이 내부 API를 호출. Public GET endpoint, 파라미터 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information Disclosure) | localStorage (Zustand persist) | accept | 탭/정렬 설정만 저장. 민감 데이터 없음. 개인 대시보드 사용 |
| T-04-02 | D (Denial of Service) | TanStack Query 과도한 리패치 | mitigate | staleTime 0 + refetchInterval 5분으로 갱신 주기 제어. retry 2회 제한. gcTime을 refetchInterval과 동일하게 설정하여 실패 캐시 적시 제거 (D-11) |
| T-04-03 | T (Tampering) | 클라이언트 상태 | accept | Zustand 스토어는 UI 상태만 관리. 퀀트 계산은 서버에서 수행(D-03). 클라이언트 변조가 데이터 정확성에 영향 없음 |
</threat_model>

<verification>
1. `npm run build` 성공
2. `npm run dev` 후 브라우저에서 http://localhost:3000 접속
3. 초기 로딩 시 Skeleton placeholder UI가 표시됨 (animate-pulse 회색 블록)
4. Market Data 섹션에 6개 시장 지표 표시 확인
5. Fear & Greed 카드에 "XX (Label)" 형태 표시 확인
6. Watchlist 테이블에 12개 종목 + RS, MA50 Dist, Drawdown, Action 컬럼 표시 확인
7. 갱신 시각 타임스탬프가 표시되고, 5분 후 자동 갱신 확인
</verification>

<success_criteria>
- TanStack Query 훅 3개가 조건부 자동 갱신 구현 (장 운영시간 5분, 장 마감 30분, market-data 항상 5분)
- D-11 준수: staleTime: 0 + gcTime: 5분으로 에러 시 stale 데이터 유지하지 않음
- DATA-07: Skeleton placeholder UI가 초기 로딩 시 표시됨 (MarketCardSkeleton, TableSkeleton)
- Zustand 스토어가 탭/검색/정렬 상태를 persist 미들웨어로 관리
- 검증 페이지에서 Phase 1 Success Criteria 4개 모두 시각적으로 확인 가능
- npm run build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-04-SUMMARY.md`
</output>
