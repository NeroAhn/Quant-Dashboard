---
phase: 01-foundation-data-pipeline
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/quant/engine.ts
  - src/lib/quant/engine.test.ts
  - src/lib/quant/signals.ts
  - src/lib/quant/signals.test.ts
  - src/lib/yahoo-finance/client.ts
  - src/lib/yahoo-finance/quotes.ts
  - src/lib/yahoo-finance/types.ts
  - src/app/api/watchlist/route.ts
  - src/app/api/monitoring/route.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-05
  - QENG-01
  - QENG-02
  - QENG-03
  - QENG-04
  - ASIG-01
  - ASIG-02
  - ASIG-03
  - ASIG-04
must_haves:
  truths:
    - "RS 계산이 S&P 500 변동률 0일 때 100을 반환한다"
    - "Buy 시그널이 Revision UP + RS > 110 + MA50 Dist < 5% 조건에서만 발생한다"
    - "Wait 시그널이 Revision DOWN OR RS < 90 (OR 로직, D-08) 조건에서 발생한다"
    - "Trim 시그널이 Buy보다 우선 평가된다 (RS > 130 + MA50 Dist > 12%)"
    - "/api/watchlist가 12개 종목 데이터를 JSON으로 반환한다"
    - "/api/monitoring이 9개 종목 데이터를 JSON으로 반환한다"
    - "개별 종목 fetch 실패 시 해당 종목만 error 필드로 표시된다 (D-02)"
  artifacts:
    - path: "src/lib/quant/engine.ts"
      provides: "RS, MA50 Dist, Drawdown 순수 계산 함수"
      exports: ["calculateRS", "calculateMA50Dist", "calculateDrawdown"]
    - path: "src/lib/quant/signals.ts"
      provides: "액션 시그널 판정 로직"
      exports: ["getDecisionAction"]
    - path: "src/app/api/watchlist/route.ts"
      provides: "GET /api/watchlist 엔드포인트"
      exports: ["GET"]
    - path: "src/app/api/monitoring/route.ts"
      provides: "GET /api/monitoring 엔드포인트"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/watchlist/route.ts"
      to: "src/lib/quant/engine.ts"
      via: "calculateRS, calculateMA50Dist, calculateDrawdown import"
      pattern: "import.*from.*quant/engine"
    - from: "src/app/api/watchlist/route.ts"
      to: "src/lib/quant/signals.ts"
      via: "getDecisionAction import"
      pattern: "import.*getDecisionAction.*from.*quant/signals"
    - from: "src/app/api/watchlist/route.ts"
      to: "yahoo-finance2"
      via: "yahooFinance.quote() 호출"
      pattern: "yahooFinance\\.quote"
---

<objective>
퀀트 엔진(RS, MA50 이격도, Drawdown)과 액션 시그널 로직을 TDD로 구현하고, Watchlist/Monitoring API 라우트를 구축한다.

Purpose: 대시보드의 핵심 가치인 "기계적 대응 시그널 자동 생성"의 계산 로직과 데이터 파이프라인을 정확하게 구현한다. 퀀트 공식은 테스트로 정확성을 보장한다.
Output: 테스트된 퀀트 엔진 + 작동하는 /api/watchlist, /api/monitoring 엔드포인트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Plan 01에서 생성된 타입/상수 계약. 이 파일들을 import하여 사용. -->

From src/lib/quant/types.ts:
```typescript
export type Revision = "UP" | "DOWN" | "NEUTRAL";
export type ActionSignal = "Buy" | "Trim" | "Wait" | "Hold";
export interface QuantMetrics { rs: number; ma50Dist: number; drawdown: number; revision: Revision; action: ActionSignal; }
export const ACTION_SIGNAL_COLORS: Record<ActionSignal, string>;
```

From src/lib/constants.ts:
```typescript
export const WATCHLIST_TICKERS = ["AAPL","GOOGL","META","MSFT","NVDA","TSLA","PLTR","IONQ","OKLO","OXY","V","UNH"] as const;
export const MONITORING_TICKERS = ["MU","AMZN","NFLX","CEG","SO","NEE","BRK-B","LLY","JNJ"] as const;
export const QUANT_THRESHOLDS = { RS_BUY: 110, RS_TRIM: 130, RS_WAIT: 90, MA50_BUY: 5, MA50_TRIM: 12, EPSILON: 1e-10 } as const;
```

From src/types/dashboard.ts:
```typescript
export interface WatchlistItem extends QuantMetrics { symbol: string; price: number|null; change1D: number|null; high52w: number|null; forwardPE: number|null; error?: string; }
export interface WatchlistResponse { data: (WatchlistItem | { symbol: string; error: string })[]; timestamp: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 퀀트 엔진 및 액션 시그널 — TDD (RED → GREEN)</name>
  <files>src/lib/quant/engine.ts, src/lib/quant/engine.test.ts, src/lib/quant/signals.ts, src/lib/quant/signals.test.ts</files>
  <read_first>
    - src/lib/quant/types.ts (Plan 01에서 생성된 타입)
    - src/lib/constants.ts (QUANT_THRESHOLDS - EPSILON, 임계값)
    - .planning/phases/01-foundation-data-pipeline/01-RESEARCH.md (Pattern 2: Pure Quant Functions, Pattern 3: Action Signal Logic)
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md (D-08: Wait OR 로직, D-09: 1D RS, D-10: MA50 소싱)
  </read_first>
  <behavior>
    ## engine.test.ts
    - calculateRS(10, 5) === 200 (정상 계산)
    - calculateRS(5, 0) === 100 (D-09: S&P 변동률 0일 때 기본값 100)
    - calculateRS(-3, 2) === -150 (음수 변동률)
    - calculateMA50Dist(105, 100) === 5.0 (정상)
    - calculateMA50Dist(100, 0) === 0 (MA50이 0일 때 안전 반환)
    - calculateDrawdown(90, 100) === -10.0 (정상)
    - calculateDrawdown(100, 100) === 0 (현재가 = 52주 고가)
    - calculateDrawdown(100, 0) === 0 (52주 고가 0일 때 안전 반환)

    ## signals.test.ts
    - ASIG-02 (Trim 우선): rs=135, ma50Dist=15, revision="UP" → "Trim" (Buy 조건도 만족하지만 Trim이 먼저)
    - ASIG-01 (Buy): rs=115, ma50Dist=3, revision="UP" → "Buy"
    - ASIG-01 조건 불충족: rs=115, ma50Dist=3, revision="NEUTRAL" → "Hold" (Revision UP 아님)
    - ASIG-03 (Wait OR - D-08): rs=85, ma50Dist=3, revision="NEUTRAL" → "Wait" (RS < 90만으로 Wait)
    - ASIG-03 (Wait OR - D-08): rs=105, ma50Dist=3, revision="DOWN" → "Wait" (Revision DOWN만으로 Wait)
    - ASIG-04 (Hold): rs=105, ma50Dist=8, revision="NEUTRAL" → "Hold"
    - 경계값: rs=110.0000000001, ma50Dist=4.9999999999, revision="UP" → EPSILON 비교로 Buy 판정 (QENG-04)
    - 경계값: rs=90, ma50Dist=5, revision="NEUTRAL" → Hold (rs가 정확히 90이면 Wait 아님, gt/lt는 strict)
  </behavior>
  <action>
**RED Phase:**
1. 테스트 러너 설정. `npm install -D vitest @types/node` (Next.js와 호환되는 경량 테스트 러너).
   vitest.config.ts 생성:
   ```typescript
   import { defineConfig } from "vitest/config";
   import path from "path";
   export default defineConfig({
     test: { globals: true },
     resolve: { alias: { "@": path.resolve(__dirname, "./src") } },
   });
   ```
   package.json에 `"test": "vitest run"` 스크립트 추가.

2. `src/lib/quant/engine.test.ts` 작성 — behavior에 정의된 8개 테스트 케이스.
3. `src/lib/quant/signals.test.ts` 작성 — behavior에 정의된 8개 테스트 케이스.
4. `npm test` 실행 — 모든 테스트 FAIL 확인 (RED).

**GREEN Phase:**
5. `src/lib/quant/engine.ts` 구현:
   ```typescript
   import { QUANT_THRESHOLDS } from "@/lib/constants";
   const { EPSILON } = QUANT_THRESHOLDS;

   export function calculateRS(tickerChangePercent: number, spChangePercent: number): number {
     if (Math.abs(spChangePercent) < EPSILON) return 100;
     return (tickerChangePercent / spChangePercent) * 100;
   }

   export function calculateMA50Dist(currentPrice: number, ma50: number): number {
     if (Math.abs(ma50) < EPSILON) return 0;
     return ((currentPrice - ma50) / ma50) * 100;
   }

   export function calculateDrawdown(currentPrice: number, high52w: number): number {
     if (Math.abs(high52w) < EPSILON) return 0;
     return ((currentPrice - high52w) / high52w) * 100;
   }
   ```

6. `src/lib/quant/signals.ts` 구현 (D-08 OR 로직, QENG-04 epsilon 비교):
   ```typescript
   import type { Revision, ActionSignal } from "./types";
   import { QUANT_THRESHOLDS } from "@/lib/constants";

   const { EPSILON, RS_BUY, RS_TRIM, RS_WAIT, MA50_BUY, MA50_TRIM } = QUANT_THRESHOLDS;

   function gt(a: number, b: number): boolean { return a - b > EPSILON; }
   function lt(a: number, b: number): boolean { return b - a > EPSILON; }

   export function getDecisionAction(rs: number, ma50Dist: number, revision: Revision): ActionSignal {
     // ASIG-02: Trim (과열) — 가장 먼저 체크
     if (gt(rs, RS_TRIM) && gt(ma50Dist, MA50_TRIM)) return "Trim";
     // ASIG-01: Buy (퀄리티 딥)
     if (revision === "UP" && gt(rs, RS_BUY) && lt(ma50Dist, MA50_BUY)) return "Buy";
     // ASIG-03: Wait (약세) — D-08: OR 로직
     if (revision === "DOWN" || lt(rs, RS_WAIT)) return "Wait";
     // ASIG-04: Hold (나머지)
     return "Hold";
   }
   ```

7. `npm test` 실행 — 모든 16개 테스트 PASS 확인 (GREEN).
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/quant/engine.ts contains "export function calculateRS("
    - src/lib/quant/engine.ts contains "export function calculateMA50Dist("
    - src/lib/quant/engine.ts contains "export function calculateDrawdown("
    - src/lib/quant/signals.ts contains "export function getDecisionAction("
    - src/lib/quant/signals.ts contains 'revision === "DOWN" || lt(rs' (D-08 OR 로직)
    - src/lib/quant/engine.test.ts contains at least 8 test/it blocks
    - src/lib/quant/signals.test.ts contains at least 8 test/it blocks
    - `npm test` exits with code 0, all tests pass
  </acceptance_criteria>
  <done>퀀트 엔진 3개 함수와 액션 시그널 로직이 16개 테스트로 검증됨. RS division-by-zero, epsilon 경계값, D-08 OR 로직 모두 테스트 커버.</done>
</task>

<task type="auto">
  <name>Task 2: Yahoo Finance 래퍼 + Watchlist/Monitoring API 라우트</name>
  <files>src/lib/yahoo-finance/client.ts, src/lib/yahoo-finance/quotes.ts, src/lib/yahoo-finance/types.ts, src/app/api/watchlist/route.ts, src/app/api/monitoring/route.ts</files>
  <read_first>
    - src/lib/quant/engine.ts (Task 1에서 생성된 퀀트 함수)
    - src/lib/quant/signals.ts (Task 1에서 생성된 시그널 함수)
    - src/lib/constants.ts (WATCHLIST_TICKERS, MONITORING_TICKERS)
    - src/types/dashboard.ts (WatchlistItem, WatchlistResponse 타입)
    - .planning/phases/01-foundation-data-pipeline/01-RESEARCH.md (Pattern 1: Server-Side Quant, Pattern 5: EPS Revision, Pitfall 2: Rate Limiting, Pitfall 4: earningsTrend 빈 응답)
  </read_first>
  <action>
1. `src/lib/yahoo-finance/types.ts` 생성 — yahoo-finance2 응답 타입 래퍼:
   ```typescript
   // yahoo-finance2의 quote() 응답에서 사용하는 필드 서브셋
   export interface YFQuoteResult {
     symbol: string;
     regularMarketPrice?: number;
     regularMarketChangePercent?: number;
     fiftyTwoWeekHigh?: number;
     fiftyDayAverage?: number;
     forwardPE?: number;
   }
   ```

2. `src/lib/yahoo-finance/client.ts` 생성 — yahoo-finance2 인스턴스 설정:
   ```typescript
   import yahooFinance from "yahoo-finance2";
   // yahoo-finance2 v3은 ESM 모듈. validation 경고 억제.
   yahooFinance.setGlobalConfig({ validation: { logErrors: false } });
   export default yahooFinance;
   ```

3. `src/lib/yahoo-finance/quotes.ts` 생성 — D-06 EPS Revision 소싱 함수 포함:
   - `batchQuote(symbols: readonly string[])`: yahoo-finance2 `quote()` 배열 호출 래퍼
   - `deriveRevision(earningsTrend)`: earningsTrend에서 Revision UP/DOWN/NEUTRAL 판정 (01-RESEARCH.md Pattern 5 코드 기반). earningsTrend 없거나 빈 응답 → "NEUTRAL" 반환.
   - `fetchTickerData(symbol, spChangePercent)`: 단일 종목의 quote + quoteSummary 호출, 퀀트 계산, 시그널 판정까지 수행. WatchlistItem 반환.

4. `src/app/api/watchlist/route.ts` 생성 (D-01, D-02, D-03, D-04):
   - GET handler
   - S&P 500(`^GSPC`) 데이터 먼저 fetch (RS 계산 기준값)
   - `WATCHLIST_TICKERS` 12개 종목에 대해 `Promise.allSettled`로 병렬 fetch
   - 각 종목: `quote()` + `quoteSummary({ modules: ["earningsTrend"] })` 호출
   - 서버에서 `calculateRS`, `calculateMA50Dist`, `calculateDrawdown`, `deriveRevision`, `getDecisionAction` 실행 (D-03)
   - 부분 실패 허용 (D-02): fulfilled → 정상 데이터, rejected → `{ symbol, error: message }`
   - 응답: `{ data: WatchlistItem[], timestamp: ISO-8601 }`
   - try/catch: 전체 실패 시 500 + `{ error: "Failed to fetch watchlist data" }`

5. `src/app/api/monitoring/route.ts` 생성 — watchlist와 동일 구조, `MONITORING_TICKERS` 9개 사용.
   코드 중복 최소화: `fetchTickerData` 헬퍼를 quotes.ts에 두고 양쪽 라우트에서 import.
   공유 함수 `buildTickerResponse(tickers, spChangePercent)` → Promise.allSettled 로직 공통화.

6. dev 서버 기동 후 `curl http://localhost:3000/api/watchlist` 및 `curl http://localhost:3000/api/monitoring`으로 응답 확인.
  </action>
  <verify>
    <automated>cd /Users/anhohwan/Desktop/AI/Quant-Dashboard && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/watchlist/route.ts contains "export async function GET("
    - src/app/api/watchlist/route.ts contains "Promise.allSettled" (D-02 부분 반환)
    - src/app/api/watchlist/route.ts contains "^GSPC" (S&P 500 RS 기준)
    - src/app/api/monitoring/route.ts contains "export async function GET("
    - src/app/api/monitoring/route.ts contains "MONITORING_TICKERS"
    - src/lib/yahoo-finance/quotes.ts contains "export function deriveRevision(" (D-06)
    - src/lib/yahoo-finance/quotes.ts contains "earningsTrend" (D-06)
    - src/lib/yahoo-finance/client.ts contains "import yahooFinance from"
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>/api/watchlist가 12개 종목 + 퀀트 메트릭 + 액션 시그널을 JSON으로 반환하고, /api/monitoring이 9개 종목으로 동일하게 작동. 개별 종목 실패 시 해당 종목만 error 표시.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Yahoo Finance API | 외부 API 호출. 응답 데이터 신뢰도, rate limiting, 서비스 중단 가능성 |
| Client → /api/* Route Handlers | 내부 API이지만 public endpoint. 입력 검증 불필요(파라미터 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | D (Denial of Service) | /api/watchlist, /api/monitoring | mitigate | Promise.allSettled로 개별 종목 실패 격리. 전체 실패 시 500 반환. Yahoo Finance rate limit 대응으로 배치 quote() 사용 |
| T-02-02 | I (Information Disclosure) | API error messages | mitigate | Yahoo Finance 원본 에러 메시지를 그대로 노출하지 않음. generic error message + 내부 로깅 |
| T-02-03 | S (Spoofing) | Yahoo Finance 응답 | accept | HTTPS 통신으로 기본 보호. yahoo-finance2가 응답 검증 내장. 개인 대시보드로 공격 표면 제한적 |
| T-02-04 | T (Tampering) | 퀀트 계산 정확성 | mitigate | 순수 함수 + 단위 테스트 16개로 계산 정확성 보장. QENG-04 epsilon 비교로 부동소수점 오차 방지 |
</threat_model>

<verification>
1. `npm test` — 16개 퀀트/시그널 테스트 전체 PASS
2. `npx tsc --noEmit` — TypeScript 컴파일 에러 없음
3. `curl http://localhost:3000/api/watchlist` — 12개 종목 JSON 반환 확인
4. `curl http://localhost:3000/api/monitoring` — 9개 종목 JSON 반환 확인
5. 응답 JSON에 `rs`, `ma50Dist`, `drawdown`, `action` 필드 존재 확인
</verification>

<success_criteria>
- 퀀트 엔진(RS, MA50 Dist, Drawdown) 3개 함수가 16개 테스트로 검증됨
- 액션 시그널 로직이 D-08 OR 로직을 정확히 구현하고 테스트됨
- /api/watchlist가 12개 종목 데이터 + 퀀트 메트릭 + 시그널을 반환
- /api/monitoring이 9개 종목 데이터를 반환
- 개별 종목 실패 시 부분 반환 작동 (D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-02-SUMMARY.md`
</output>
